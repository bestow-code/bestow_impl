// ignore_for_file: prefer_const_constructors
import 'package:bestow_impl/bestow_impl.dart';
import 'package:test/test.dart';

void main() {
  group('BestowImpl', () {
    test('can be instantiated', () {
      expect(BestowImpl(), isNotNull);
    });
  });
}
